# Anthropic API key — used ONLY server-side by /app/api/coach-ai (Phase 5).
# Never expose this client-side. Copy this file to `.env.local` and fill in.
ANTHROPIC_API_KEY=sk-ant-...

# Model used for the AI coaching chat (defaults to claude-sonnet-4-6 if unset).
COACH_AI_MODEL=claude-sonnet-4-6
